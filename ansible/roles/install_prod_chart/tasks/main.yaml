- name: Install Prod Helm Chart
  shell: |
    "helm install prod-myapp /home/vagrant/Django_final_project/kubernetes/production_helm_chart" \
    --wait --timeout 3m --atomic
  args:
    executable: /bin/bash
